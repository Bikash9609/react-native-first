import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  listLeftIcon: {
    marginHorizontal: 6,
  },
  listBodyText: {
    marginHorizontal: 10,
    textAlign: 'left',
  },
});
