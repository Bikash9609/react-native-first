import React from "react";
import { StyleSheet, Text, View } from "react-native";

const auth = () => {
	return (
		<View>
			<Text></Text>
		</View>
	);
};

export default auth;

const styles = StyleSheet.create({});
